#include <stdio.h>

int main()
{
	int T, i, octa;
	char d[100], b[100];

	scanf(" %d", &T);

	for(i = 0; i < T; i++) {
		scanf(" %s %s", d, b);

		for(j = 0, octa = 1; j <= 34; j++) {
			if(b[j] != 0)
		}



	}


}